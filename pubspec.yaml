name: dojofit
description: "A new Flutter project."
publish_to: 'none' 
version: 0.6.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  equatable: ^2.0.8
  dartz: ^0.10.1
  mocktail: ^1.0.5
  pop_network: ^1.1.2
  injector: ^4.0.0
  auto_injector: ^2.1.1
  bloc_test: ^10.0.0
  flutter_bloc: ^9.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/api/mock/
